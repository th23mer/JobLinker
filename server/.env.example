# Server
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=project_db

# Auth
JWT_SECRET=change-me-to-a-long-random-string

# SMTP (email notifications — optional in development)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=your-smtp-password
SMTP_FROM="JobLinker <no-reply@example.com>"
